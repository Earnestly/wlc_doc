WLC_HANDLE_GET_USER_DATA(3)                              WLC CORE API FUNCTIONS

NAME

    wlc_handle_get_user_data - get user data associated with the handle

SYNOPSIS

    void* wlc_handle_get_user_data(wlc_handle handle);

DESCRIPTION

    wlc_handle_get_user_data() provides a mechanism to aquire the user data
    provided via wlc_handle_set_user_data(3) allowing for direct access to its
    contents.

RETURN VALUE
    
    wlc_handle_get_user_data() returns a pointer to the address of the user
    data.

    If no user data has been set then wlc_handle_get_user_data() will return
    NULL.

ALSO SEE

    wlc_handle_set_user_data(3)
